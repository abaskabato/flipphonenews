{
  "name": "deaddrop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DEAD DROP — a 3D WebGL flip phone that buries anonymous messages at real places. Reading is free; leaving a pin is a paid Stripe micro-transaction.",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "stripe": "^17.5.0"
  }
}
